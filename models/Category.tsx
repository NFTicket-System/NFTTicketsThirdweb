export class Category {

    constructor(id: number, libelle: string, color: string) {
        this.id = id
        this.libelle = libelle
        this.color = color
    }

    id: number
    libelle: string
    color: string
}
